name = "vanko-api"
main = "src/index.ts"
compatibility_date = "2024-02-01"

[vars]
ENVIRONMENT = "production"

# Cloudflare Workers Cron Trigger: Automated deletion reconciliation & audit purging every 30 minutes
[triggers]
crons = ["*/30 * * * *"]

# Secrets (Set via `npx wrangler secret put GROQ_API_KEY`):
# GROQ_API_KEY
